{
  "name": "newshub",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:migrate": "pnpm --filter @newshub/db migrate",
    "db:seed": "pnpm --filter @newshub/db seed"
  },
  "devDependencies": {
    "turbo": "^2.9.5"
  }
}
